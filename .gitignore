# Secrets & environment variables — NEVER commit these
.env
.env.local
.env.*.local
# WhatsApp session data — contains your login!
.wwebjs_auth/
.wwebjs_cache/
# Node.js dependencies — too big, can be reinstalled with `npm install`
node_modules/
# Logs
*.log
npm-debug.log*
# OS-specific junk
.DS_Store
Thumbs.db
# VS Code workspace settings (optional, depending on team)
.vscode/
# History file we'll create later (contains personal medical data!)
history.json
# Personal medication list — medical info!
pills.json

index.old.js